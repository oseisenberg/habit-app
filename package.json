{
  "name": "habit-app",
  "version": "1.0.0",
  "private": true,
  "description": "Habit tracker PWA, packaged for iOS with Capacitor",
  "scripts": {
    "build:www": "bash scripts/build-www.sh",
    "test": "node tests/run.js",
    "cap:sync": "npm run build:www && npx cap sync ios",
    "ios:open": "npm run cap:sync && npx cap open ios"
  },
  "dependencies": {
    "@capacitor/core": "^6.1.0",
    "@capacitor/ios": "^6.1.0",
    "@capacitor/local-notifications": "^6.1.0",
    "@capawesome/capacitor-badge": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.0"
  }
}
